
# Skip the problematic react-email packages
omit=@react-email/components,@react-email/render
# Use legacy peer deps to handle any remaining issues
legacy-peer-deps=true
# Force installs to proceed despite conflicts
force=true
